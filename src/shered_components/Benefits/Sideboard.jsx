import React from 'react'


export default function Sideboard({class}) {
  return (
 
  <div className={styles.class}>
    <p>{title}</p>
    <h1>{heading}</h1>
    <img src={vectorimg} />
    <p>{description}</p>
  </div>
  )
}
